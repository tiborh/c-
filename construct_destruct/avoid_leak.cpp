#include <iostream>

struct collector {
  int len;
  collector(): 
};

using namespace std;

int main() {
  // to be finished: message: dynamic allocation needs to be freed up before concat creates new.
  return 0;
}
