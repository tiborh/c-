#include <iostream>

using namespace std;

int multiplyBy10(int number)
{
    stringstream str;
    str << number << '0';
    str >> number;
    return number;
}

int main()
{
  for (int i = 0; i < 11; i++)
    cout << multiplyBy10(i) << endl;

  return 0;
}
