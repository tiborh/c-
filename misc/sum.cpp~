#include <iostream>
using namespace std;

int main()
{
  int sum, i, j, k;
  for (sum = i = 0, j = 2, k = i + j; k < 10; i++, j++, k++)
    sum += (i < j) ? k : i;
  cout << "sum == " << sum << endl;

  return 0;
}
