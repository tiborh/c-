#include <iostream>
#include <iterator>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

int main() {
  ifstream data_file("/iostudy/txt/frost_theRoadNotTaken.txt");
  istream_iterator<int> start(data_file), end;
  vector<string> data(start,end);
  
  return 0;
}
